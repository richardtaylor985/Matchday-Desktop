# Legacy local server

`server.js` is retained temporarily as a rollback/reference implementation.
Stage 2.5c normal operation does not use it.

The client calls the production Vercel API directly and no longer needs a
football-data.org API key or local Node.js proxy.
